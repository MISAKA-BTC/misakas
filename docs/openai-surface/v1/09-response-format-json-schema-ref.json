{
  "request": {
    "messages": [
      {
        "role": "user",
        "content": "Extract the address."
      }
    ],
    "response_format": {
      "type": "json_schema",
      "json_schema": {
        "name": "with_ref",
        "schema": {
          "type": "object",
          "properties": {
            "address": {
              "$ref": "#/components/schemas/Address"
            }
          },
          "required": [
            "address"
          ]
        }
      }
    }
  },
  "expect": {
    "verdict": "refused",
    "reason_contains": "`$ref` at \"/properties/address\"",
    "studio": {
      "verdict": "accepted",
      "why": "the Studio checks response_format for shape only and hands the schema to the engine (Decision 1's table: the engine's); the lane's subset is the gateway's rule, applied when the request reaches it"
    }
  }
}
