# misaka-next — 作業状況まとめ (2026-09-25 21:40 JST, エージェント停止時点)

トークン残量の都合で全エージェントを停止した。このファイルは、それまでに動いたエージェントの結果をすべてまとめたもの。

## 1. 基盤
- branch `next/misaka-next`(`rcore/int-3 @ a0af3c92` = t12 参照実装から分岐)
- worktree `/Users/wata/Downloads/MISAKA-next`(scratchpad ではないので永続)
- 新 workspace は `misaka-next/`。`README.md` と `PROVENANCE.md`(参照 commit と未 merge branch 4 本)を commit 済み(fc6b2aca)
- t12 のテストバイナリ(kaspa-consensus-core / kaspa-consensus)は `wt-int2-target` で事前ビルド済み

## 2. Workflow wf_c954c4e9-29d「Consensus Book」— 途中停止
| 段階 | エージェント | 状態 |
|---|---|---|
| Extract | 章の執筆者 ×5(time / claims-pol / bonds-econ / panel / fork-final)+ 攻撃史担当 ×1 | 完了 |
| Synthesize | 統合担当(09 / 10 / 00 と整合修正) | 完了 |
| Review | レビュー担当 ×1 | 完了: **blocker 3・major 15・minor 5** |
| Fix | 修正担当 ×1 | **途中で停止**(一部だけ反映。`11-block-and-transition.md` の新設もこの段階) |

成果物 `misaka-next/docs/consensus/`:
00-overview, 01-time-and-daa, 02-bonds, 03-claims, 04-proof-of-llm, 05-panel-validation, 06-fork-choice, 07-finality, 08-economics, 09-invariants(invariant 86 件), 10-attack-model(攻撃 184 件), 11-block-and-transition(修正担当が途中まで作成), appendix-a-attack-catalog

### 2.1 seed 攻撃 5 件の t12 判定(統合担当)
| 攻撃 | t12 判定 | 要点 |
|---|---|---|
| private_fake_root_burst | **partial** | hash だけで ticket を grind できる(palw_attempt_v2.rs:562-589, params.rs:2457-2465)。panel 前に blue work・live weight・frontier の保持・W カウントが付く。safe weight と mint は付かない。2 回目の失敗で forfeit |
| private_daa_finality_acceleration | **partial** | DAA 自体は壁時計ペース。ただし Final は branch の DAA で判定し(palw_state_v2.rs:23764-23771)、比較の第 1 キーは blue score(palw_fork_choice.rs:72-78)。第二時計の抜け道もある(:2224-2229) |
| heartbeat_clock_acceleration | **partial** | 速度は floor H3/H5 で閉じている。ただし heartbeat が唯一の時計源で、blue-score 深度と DNS TTL にも効く。mint はしない |
| bond_split_amplification | **partial** | 発行・ticket・上限は中立。split に敏感なのは seat 抽選(operator 1 人 1 席・1M cap)、tier、strike、ceil(c/2) の share、64 reporter commitment |
| failed_lottery_blue_weight | **real** | header だけで 2^20 の work が付く(protocol.rs:666-671)。Layer-0 は無条件(pow/src/lib.rs:591-596)。blue work が sink heap と selected parent を決める(processor.rs:13636-13644)。PALW weight 側には効かない |

これ以外に「real」と判定した主なもの: panel_anchor_regrind / panel_draw_seed_grind(anchor 生産者が seed を grind できる)、admission_jury_seed_grind、second_clock_heartbeat_escape、private_absence_conviction、private_work_target_easing、unverified_live_weight、path_dependent_sink_split、finality_ttl_release、settled_claim_reverted_with_branch、forgers_race(修正は pending 中)、readiness_proof_outsourced、held_attention_lie_unattributable、panel_root_claim_arity_mismatch、silent_sink_burn、action_tier_dilution、strike_evasion_by_split、early_extraction、weight_unit_gap。
**注意:** レビューの結果、`anchor_bind_censorship` の「real」は誤読。PanelBound は各ノードが acceptance 時に自分で導出する(processor.rs:11814-11828)ので、anchor 生産者が省略することはできない。

### 2.2 統合時の主な設計判断(owner の確認が要る)
- **fork choice は時計を一切読まない。** seed 案の common_safe_daa は、pairwise の min だと非推移的になり、候補集合全体の min だと安い候補 1 つに引きずられる。そこで SafeContext は anchor だけで作る。比較器は各候補が自分の key を持つ strict total order(レビューでも確認)。
- SafeDaa は licence ring で定義する(01 の定義を採用)。06 の「frontier ブロックの LocalDaa」という定義は循環するため不採用。
- 期限の起点(mark)は、生成ブロックの LocalDaa。受付・異議・turn・開示の期限は `Deadline<Safe>` で測る。bind の期限だけ `Deadline<Local>`。
- β_u = 0(未検証 claim の live weight はゼロ)、β_v = 1/2。safe weight は d_bury だけ埋まってから数える。

### 2.3 レビューの blocker(未修正のまま残っている可能性がある)
1. 10-attack-model の `anchor_bind_censorship` 判定が誤読(上記)。
2. `FinalizedDaa` が「ノード自身の anchor」と「ブロック相対の anchor」の両方を指している。このため chain-state の規則(SafeDaa の floor、vesting、exit、登録の成熟)がノードの履歴に依存してしまう。型を 2 つに分けること。
3. 01 の S4「heartbeat を挿入しても safe_daa は不変」は偽。ring が licence ブロックの LocalDaa を保存しているため。このままだと INV-CLAIM-01 / INV-FORK-01 / INV-TIME-06 の想定テストが落ちる。

### 2.4 major の主なもの
- vesting と exit の時計が章ごとに食い違う(01 と 03)。
- `Deadline<C>` を作る手段が複数あり、SafeDaa を起点にした期限が作れてしまう。
- UnverifiedClaim / VerifiedClaim の定義が 03 と 05 で違う。frontier に未検証 claim を含めるかどうかも 03・06・07 で揺れている。
- heartbeat の ε weight(01)と「weight はゼロ」(06・10)が矛盾している。
- 供給上限 `genesis + Σsubsidy ≤ MAX_SUPPLY` が規則から導けない(08)。
- ρ(weight の上限)を定数にする必要があるが、step_work_target に上限がない(06)。
- seed の規則が未確定で、02 の「account は identity ではない」と矛盾する(04)。
- 復元抽選だと、1 account が同じ segment で複数の seat を取れる(05)。
- 時計センサスの漏れ(readiness freshness の 8 spans など)。
- block 構造(DAG・mergeset)の章がない → 11 章を途中まで作成済み。
- DaaSpan の窓と finality / pruning 深度を直接比べている不等式が 3 つある(05)。
- 09 の live−safe の上界式が 06 の式と合わない。

### 2.5 owner 向けの未決定事項(統合担当の OQ-1〜25、推奨案付き)
09 / 00 の各章の "Open questions" に記載。主なもの:
- OQ-1: panel と jury の seed(K=4、1 単位遅らせる)
- OQ-2: β_u=0
- OQ-4: licence が止まったときの扱い
- OQ-6: 復元抽選
- OQ-9: fake root は最初の panel で有罪にする
- OQ-11: 報酬の解放時点
- OQ-13: ticket の target を header の段階で検証できるか
- OQ-14: 外部の finality overlay は置かない
- OQ-16: 2M は閉じたまま
- OQ-17: 時計のパラメータ(SLOT 120s)
- OQ-18: DAG を採用し、blue-score 深度は廃止
- OQ-21: optimistic licence(S2)を廃止

未解決のまま残っているもの:
- seed の bootstrap(POL-R9)
- licence が止まったときに exit できない
- zero_weight_lane_hash_tiebreak の再発
- FORK-R16 の較正で +b が抜けている
- 一部の判定がまだコード未確認(quantum_maturity_reads_wall_clock ほか)
- pending branch 4 本は統合段階で読み直していない

## 3. Workflow wf_451ff193-9a2(bind 省略 probe)
誤送信されたメッセージに基づいて起動したもの。すぐに停止し、t12 側の変更はない。

## 4. 次にやること
1. 修正担当の続き。2.3 の blocker 3 件と 2.4 の major 15 件を直し、11 章を仕上げる。レビュー結果の全文は workflow の journal にある: `~/.claude/projects/-Users-wata-Downloads-MISAKA-testnet/8fdbafea-.../subagents/workflows/wf_c954c4e9-29d/journal.jsonl`。
2. owner が OQ-1〜25 を決める。
3. primitives → consensus model → adversarial simulator → consensus tests(攻撃ごとに回帰テストを 1 本)。
4. t12 側で probe を書き、判定を実測で確定させる(特に partial / real のもの)。
