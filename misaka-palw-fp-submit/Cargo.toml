[package]
name = "misaka-palw-fp-submit"
description = "ADR-0077 Decision 4: the ONE submit path a free-prompt commitment takes — funding selection that asks the mempool, the subnetwork check, the material staged with the partial+rename discipline, and the 0x4a broadcast. Called by `misaka palw fp-submit` (the manual form) and by misaka-palw-gateway (the automatic one)."
publish = false
rust-version.workspace = true
version.workspace = true
edition.workspace = true
authors.workspace = true
include.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
kaspa-addresses.workspace = true
kaspa-consensus-core.workspace = true
kaspa-hashes.workspace = true
# `is_spendable_settled`: both coinbase gates, so this selector and the wallet's cannot disagree.
kaspa-pq-validator-core.workspace = true
kaspa-rpc-core.workspace = true
kaspa-txscript.workspace = true

borsh.workspace = true

[dev-dependencies]
# `block_on`, so the SHIPPED handoff ordering is what the tests drive — not a sync copy of it.
futures.workspace = true

[lints]
workspace = true
