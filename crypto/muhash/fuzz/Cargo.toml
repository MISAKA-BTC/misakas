[package]
name = "muhash-fuzz"
version = "0.0.0"
authors = ["Automatically generated"]
publish = false
edition = "2021"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"

[dependencies.muhash]
path = ".."
package = "kaspa-muhash"

# Prevent this from interfering with workspaces
[workspace]
members = ["."]

# kaspa-pq TODO (Phase 3): add an LtHash fuzz target that exercises
# `add_element`/`remove_element` ordering, `combine`/`subtract` inverses,
# and `serialize`/`deserialize` roundtrips. The upstream `u3072` fuzz target
# was deleted alongside the removal of the multiplicative MuHash backing
# field.
